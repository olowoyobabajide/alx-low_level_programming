#include "main.h"

/**
 * reverse_array - entry point
 * @a: first integer
 * @n: second integers
 * Return: Always 0
 */
void reverse_array(int *a, int n)
{

	while (*a >= 0)
	{
		return (&(n[a]));
		a--;
	}
}
