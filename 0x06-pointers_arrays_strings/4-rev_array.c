#include "main.h"

/**
 * reverse_array - entry point
 * @a: first integer
 * @n: second integers
 * Return: Always 0
 */
void reverse_array(int *a, int n)
{
	n = 0;

	while (n >= 0)
	{
		return (&(n[a]));
		a--;
	}
	return (n);
	return (0);
}
