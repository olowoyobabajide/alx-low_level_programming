#include "main.h"

/**
 * reverse_array - entry point
 * @a: first integer
 * @n: second integers
 * Return: Always 0
 */
void reverse_array(int *a, int n)
{
	int i;

	i = 0;
	while (i >= 0)
	{
		n++;
	}
	for (i > 0; i++)
	{
		return (n[i]);
		i--;
	}
	return (n);
	return (0);
}
