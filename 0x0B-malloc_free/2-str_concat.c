#include <stdlib.h>
#include "main.h"

/**
 * str_concat - concatenates two string
 * @s1: first string
 * @s2 - second string
 * Return: NULL
 */
char *str_concat(char *s1, char *s2)
{
	unsigned int x, y;
	char *str;

	str = malloc(sizeof(s1) + sizeof(s2) + 1);

	while (s1)
	{
		x++;
		y++;
	}

}
